const allConfig = require('./config.all');
const commonConfig = require('./config.common');
const reactConfig = require('./config.react');

module.exports = {
  all: allConfig,
  common: commonConfig,
  react: reactConfig,
};
